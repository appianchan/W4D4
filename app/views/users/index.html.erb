<h1>These are our users</h1>

<a href="<%= new_user_url %>">Create new User</a>
<ul>
    <% @users.each do |single_user| %>
        <li> 
            <%= render 'user', user: single_user, action: :index %>
        </li>
        <br>
    <% end %>
</ul>